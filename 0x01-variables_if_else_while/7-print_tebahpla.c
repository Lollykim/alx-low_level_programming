#include <stdio.h>
/**
 * main - program that prints the lower alphabet in reverse
 *
 * Return; 0 if Success
 */

int main(void)
{
int a;

for (a = 122 ; a >= 97 ; a++)
{
putint(a);
}

putint('\n');
return (0);
}
